name: Platform Visual QA

on:
  push:
    branches:
      - integration/a6-platform-shell
  pull_request:
    paths:
      - "apps/web/app/platform/**"
      - "apps/web/features/platform/**"
      - "apps/web/messages/**"
      - "scripts/visual-qa/platform-production-visual.mjs"
      - ".github/workflows/platform-visual-qa.yml"

permissions:
  contents: read

jobs:
  platform-visual-qa:
    name: Platform Desktop Mobile RTL Evidence
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.28.1 --activate
          pnpm --version

      - name: Install repository dependencies
        run: pnpm install --frozen-lockfile

      - name: Check Platform visual harness
        run: node --check scripts/visual-qa/platform-production-visual.mjs

      - name: Install isolated Playwright
        run: |
          mkdir -p /tmp/balcona-platform-visual-qa
          cd /tmp/balcona-platform-visual-qa
          npm init -y
          npm install --no-audit --no-fund playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Start Balcona web
        env:
          NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:3999/api/v1
          NEXT_PUBLIC_APP_ENV: test
          NEXT_PUBLIC_GIT_SHA: ${{ github.sha }}
        run: |
          pnpm web:dev > /tmp/balcona-platform-web.log 2>&1 &
          echo $! > /tmp/balcona-platform-web.pid

          for attempt in $(seq 1 60); do
            if curl --fail --silent http://localhost:3001/platform/login > /dev/null; then
              exit 0
            fi
            sleep 1
          done

          cat /tmp/balcona-platform-web.log
          exit 1

      - name: Capture Platform visual evidence
        env:
          BALCONA_VISUAL_BASE_URL: http://localhost:3001
        run: node scripts/visual-qa/platform-production-visual.mjs

      - name: Upload Platform visual evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: platform-production-visual-qa
          path: artifacts/platform-visual-qa
          if-no-files-found: error
          retention-days: 14

      - name: Print web log on failure
        if: failure()
        run: cat /tmp/balcona-platform-web.log || true
