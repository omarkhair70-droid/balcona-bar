{
  "name": "balcona-bar",
  "version": "0.1.0",
  "private": true,
  "description": "Balcona Bar monorepo",
  "workspaces": [
    "apps/api"
  ],
  "scripts": {
    "api:dev": "npm run start:dev -w apps/api",
    "api:build": "npm run build -w apps/api",
    "api:test": "npm run test -w apps/api",
    "api:prisma:generate": "npm run prisma:generate -w apps/api"
  },
  "engines": {
    "node": ">=22"
  }
}
